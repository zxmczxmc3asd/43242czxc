print("=== DEBUG START ===")

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
print("1 - player ok:", player ~= nil)

local stats = player:FindFirstChild("leaderstats")
print("2 - leaderstats:", stats ~= nil)

if stats then
    local reb = stats:FindFirstChild("Rebirths")
    print("3 - Rebirths:", reb ~= nil)
    if reb then print("4 - Rebirths value:", reb.Value) end
else
    print("3 - SEM LEADERSTATS - esse e o problema!")
end

local cam = workspace.CurrentCamera
print("5 - Camera:", cam ~= nil)
if cam then print("6 - ViewportSize:", cam.ViewportSize.X, cam.ViewportSize.Y) end

print("7 - TouchEnabled:", UserInputService.TouchEnabled)

-- testa criar GUI simples
local gui = Instance.new("ScreenGui")
gui.Name = "TestGui"
gui.Parent = player.PlayerGui
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0.5, 0, 0.5, 0)
frame.Position = UDim2.new(0.25, 0, 0.25, 0)
frame.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
frame.Parent = gui
print("8 - GUI de teste criada - apareceu um quadrado vermelho na tela?")

task.wait(3)
gui:Destroy()
print("=== DEBUG END ===")
